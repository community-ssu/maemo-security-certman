#!/bin/sh
if [ -f /usr/lib/microb-engine/libnssckbi.so ] ; then
    ln -sf /usr/lib/libmaemosec_certman.so.0.0.0 /usr/lib/microb-engine/libnssckbi.so
else
    echo Warning: microb-engine not installed yet
fi
ldconfig
#DEBHELPER#
